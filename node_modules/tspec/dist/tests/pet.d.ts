import { RequestHandler } from 'express';
import { Tspec } from '../index';
interface Pet {
    id: number;
    category: {
        id: number;
        name: string;
    };
    name: string;
    photoUrls: string[];
    tags: {
        id: number;
        name: string;
    }[];
    status: 'available' | 'pending' | 'sold';
}
interface GetPetPathParams {
    /**
     * pet id
     * @examples [1234]
     * */
    id: string;
}
declare const getPet: RequestHandler<GetPetPathParams, Pet>;
export declare type PetApiSpec = Tspec.DefineApiSpec<{
    basePath: '/pet';
    paths: {
        '/{id}': {
            get: {
                summary: 'Find pet by ID';
                handler: typeof getPet;
            };
        };
    };
}>;
export {};
